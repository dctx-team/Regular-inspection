playwright>=1.40.0
httpx[http2]>=0.25.0
python-dotenv>=1.0.0
pytz>=2024.1
pyotp>=2.8.0
cryptography>=41.0.0
pyyaml>=6.0.0
cloudscraper>=1.2.71

# 可选依赖 - TLS 指纹伪装（2025 高级 Cloudflare 绕过）
# 注意：curl-cffi 可能需要编译依赖，安装失败不影响基础功能
# Windows: 需要 Visual C++ Build Tools
# Linux: 需要 libcurl-dev 和 python3-dev
# 安装命令：pip install curl-cffi>=0.6.0 --prefer-binary
# 启用方式：设置环境变量 ENABLE_TLS_FINGERPRINT=true
curl-cffi>=0.6.0

# 测试依赖
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0